apiVersion: 1

providers:
  - name: SmartBack
    orgId: 1
    folder: SmartBack
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
